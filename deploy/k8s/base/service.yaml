apiVersion: v1
kind: Service
metadata:
  name: muse-spark
  labels:
    app.kubernetes.io/name: muse-spark
spec:
  selector:
    app.kubernetes.io/name: muse-spark
  ports:
    - name: http
      port: 80
      targetPort: http
